{
  "name": "slides-variable-font",
  "version": "0.1.0",
  "private": true,
  "description": "Google Slides add-on for designing with variable typefaces and inserting the result as an editable Slides shape.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "preview": "node tools/build-preview.mjs",
    "push": "clasp push",
    "open": "clasp open",
    "build": "node tools/build-appsscript.mjs"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "fast-xml-parser": "^5.0.0"
  }
}
